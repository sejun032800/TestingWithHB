피카소 봇

Images 폴더에 있는 이미지 흑백으로 그림판에다 그리는 코드

Aeuigh 폴더에 있고 CMD에서 실행해야 함
OpenCV 써서 버튼 찾는데 그림 제대로 안 그려지면 색깔 바꾸는 버튼을 OpenCV가 병신돼서 못 찾는 거임

그렇면 mousePos.py로 색깔 바꾸는 버튼 좌표(1), 색 중 R값 설정 칸 좌표(2), '확인' 버튼 좌표(3) 찾아서 직접 설정해야 하는데, ##----------INPUT COORD HERE (1, 2, 3)------------ 바로 밑 줄에 center = (x좌표, y좌표), red = (x, y), button = (x, y) 하면 됨